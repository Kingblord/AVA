export async function getCustomerHistory(customer_id: string) {
  // Replace with Firestore later
  return {
    success: true,
    customer_id,
    total_orders: 5,
    total_spent: 12000,
    last_order: "Instagram followers"
  };
}
